#include "URL.h"

const std::string URL::filename = "urls.cache";

bool URL::validateURL(std::string url) {
	std::cout << fileLines(filename).size() << std::endl; 

	return true;
}

bool URL::validateSelection(int line) {

	return true;

}
